'use strict';
Object.defineProperty(exports, "__esModule", { value: true });
const fs = require("fs");
const path = require("path");
exports.default = ({ type }) => {
    const validPath = path.resolve(__dirname, `../data/${type}/valid/`);
    const validTestFiles = fs.readdirSync(validPath);
    const validTests = validTestFiles.map((file) => fs.readFileSync(path.resolve(validPath, file), 'utf-8')).map(jsonString => JSON.parse(jsonString));
    return validTests;
};
//# sourceMappingURL=loadTests.js.map